Quiniela F1 V2.1 - Completa (login, auto-close, auto-assign, puntos, standings, histórico)

Instalación:
  python -m venv .venv
  source .venv/bin/activate   (Linux/mac) OR .venv\Scripts\activate (Windows)
  pip install -r requirements.txt

Ejecución:
  streamlit run app.py
